// Stub for @prisma/client in frontend builds
// The generated Zod schemas import Prisma types, but we don't need the actual client in the browser

export const Prisma = {
  JsonNull: 'JsonNull' as const,
  DbNull: 'DbNull' as const,
  AnyNull: 'AnyNull' as const,
};

export default {};